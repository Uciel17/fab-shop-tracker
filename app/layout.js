export const metadata = {
  title: 'Fab Shop Tracker',
  description: 'Project management for fabrication shop',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <script src="https://cdn.tailwindcss.com"></script>
      </head>
      <body>{children}</body>
    </html>
  )
}